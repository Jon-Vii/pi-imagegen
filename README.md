# pi-imagegen

Pi extension for OpenAI/Codex subscription image generation (`gpt-image-2`) plus a local browser studio.

## Development

```bash
pi -e ./imagegen.ts
```

Or copy/symlink `imagegen.ts` into `~/.pi/agent/extensions/` and run `/reload`.
